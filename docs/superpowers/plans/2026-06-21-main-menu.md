# 主菜单 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 加一个白盒主菜单作为启动场景（标题 + 悬赏解锁进度 + 出航 + 退出），点「出航」才开始新 run。

**Architecture:** 新增 `MainMenu.tscn`（Control 根 + 脚本，UI 在 `_ready` 代码构建，白盒，与 RouteScene 同范式）+ `main_menu.gd`。`project.godot` main_scene 改为 MainMenu。出航经 `SceneManager.goto_route()`（RouteScene IDLE 自动起航）。导航/退出走可注入 Callable 接缝以便测试。SceneManager / RouteScene 不改。

**Tech Stack:** Godot 4.6.3 / GDScript / GdUnit4。

## Global Constraints

- 引擎 Godot 4.6.3；测试前必 `godot --headless --import`；二进制 `/Applications/Godot.app/Contents/MacOS/Godot`。
- 静态类型纪律：禁 `var x := <Variant 表达式>`（如 `auto_free(...)`）；用显式标注 / `as T`。typed Array 字面量赋 typed 属性须先建 typed 局部。（注：`var x := [0]` 推断为 Array，非 Variant，允许。）
- 白盒 UI：只用按钮，先 `add_child` 再 `set_anchors_preset`。
- UI 场景脚本可声明 class_name（非 autoload）。
- 中文对话；标识符英文。trunk-based 在 main。
- 提交 Conventional Commits，body 带 `Story: main-menu`，结尾 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

### Task 1: MainMenu 启动场景

**Files:**
- Create: `src/ui/main_menu.gd`
- Create: `scenes/MainMenu.tscn`
- Modify: `project.godot`（`run/main_scene` → MainMenu）
- Test: `tests/integration/main_menu/main_menu_test.gd`

**Interfaces:**
- Consumes: `SceneManager.goto_route()`；`MetaProgress.unlocked_crew_ids: Array[String]` / `get_unlock_order() -> Array[String]`（=3 个 unlockable）；`get_tree().quit()`。
- Produces: `MainMenu`（class_name，Control）成员 `_set_sail_button: Button` / `_quit_button: Button` / `_unlock_label: Label` / `_nav_set_sail: Callable` / `_nav_quit: Callable`；方法 `_on_set_sail()` / `_on_quit()` / `_default_set_sail()` / `_default_quit()`。

- [ ] **Step 1: 写失败测试**

创建 `tests/integration/main_menu/main_menu_test.gd`：

```gdscript
# 主菜单集成测试（白盒）：渲染 + 解锁进度 + 导航/退出接缝（不真的切场景/退出）。
extends GdUnitTestSuite

func before_test() -> void:
	MetaProgress.unlocked_crew_ids.clear()

func after_test() -> void:
	MetaProgress.unlocked_crew_ids.clear()

# AC-1：渲染出航/退出按钮 + 解锁进度（清空时 0/3）。
func test_renders_buttons_and_unlock_progress() -> void:
	var mm: MainMenu = auto_free(MainMenu.new())
	add_child(mm)
	assert_bool(mm._set_sail_button != null).is_true()
	assert_bool(mm._quit_button != null).is_true()
	assert_str(mm._unlock_label.text).is_equal("悬赏解锁 0 / 3")

# AC-2：解锁进度反映 MetaProgress。
func test_unlock_progress_reflects_metaprogress() -> void:
	var ids: Array[String] = ["crew_gunner_03", "crew_medic_02"]
	MetaProgress.unlocked_crew_ids = ids
	var mm: MainMenu = auto_free(MainMenu.new())
	add_child(mm)
	assert_str(mm._unlock_label.text).is_equal("悬赏解锁 2 / 3")

# AC-3：出航触发导航接缝。
func test_set_sail_invokes_nav_seam() -> void:
	var mm: MainMenu = auto_free(MainMenu.new())
	add_child(mm)
	var called := [0]
	mm._nav_set_sail = func() -> void: called[0] += 1
	mm._on_set_sail()
	assert_int(called[0]).is_equal(1)

# AC-4：退出触发退出接缝。
func test_quit_invokes_quit_seam() -> void:
	var mm: MainMenu = auto_free(MainMenu.new())
	add_child(mm)
	var called := [0]
	mm._nav_quit = func() -> void: called[0] += 1
	mm._on_quit()
	assert_int(called[0]).is_equal(1)

# AC-5：main_scene 指向 MainMenu。
func test_main_scene_is_main_menu() -> void:
	var scene: String = str(ProjectSettings.get_setting("application/run/main_scene"))
	assert_str(scene).is_equal("res://scenes/MainMenu.tscn")
```

- [ ] **Step 2: 跑测试确认失败（RED）**

```bash
cd "/Users/chris/Documents/First Codex/my-game"
"/Applications/Godot.app/Contents/MacOS/Godot" --headless --import 2>&1 | tail -2
"/Applications/Godot.app/Contents/MacOS/Godot" --headless -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode --continue -a res://tests/integration/main_menu 2>&1 | grep -iE "FAILED|error|Statistics|SCRIPT ERROR" | tail -8
```
Expected: 失败（`MainMenu` 类不存在 / main_scene 仍为 RouteScene）。

- [ ] **Step 3: 创建 main_menu.gd**

`src/ui/main_menu.gd`：

```gdscript
# 主菜单（启动场景，白盒 Control）。标题 + 悬赏解锁进度 + 出航 + 退出。
# 导航/退出经可注入 Callable 接缝（DI over singleton），测试覆盖为 no-op。
class_name MainMenu
extends Control

var _set_sail_button: Button = null
var _quit_button: Button = null
var _unlock_label: Label = null

var _nav_set_sail: Callable
var _nav_quit: Callable
func _default_set_sail() -> void: SceneManager.goto_route()
func _default_quit() -> void: get_tree().quit()

func _ready() -> void:
	_nav_set_sail = _default_set_sail
	_nav_quit = _default_quit
	var box := VBoxContainer.new()
	add_child(box)
	box.set_anchors_preset(Control.PRESET_CENTER)
	var title := Label.new()
	title.text = "《孤帆棋海》"
	box.add_child(title)
	_unlock_label = Label.new()
	_unlock_label.text = "悬赏解锁 %d / %d" % [MetaProgress.unlocked_crew_ids.size(), MetaProgress.get_unlock_order().size()]
	box.add_child(_unlock_label)
	_set_sail_button = Button.new()
	_set_sail_button.text = "出航"
	_set_sail_button.pressed.connect(_on_set_sail)
	box.add_child(_set_sail_button)
	_quit_button = Button.new()
	_quit_button.text = "退出"
	_quit_button.pressed.connect(_on_quit)
	box.add_child(_quit_button)

# 出航 → RouteScene（IDLE 自动起航）。经接缝避免测试真的切场景。
func _on_set_sail() -> void:
	_nav_set_sail.call()

# 退出 → 关闭游戏。经接缝避免测试退出运行器。
func _on_quit() -> void:
	_nav_quit.call()
```

- [ ] **Step 4: 创建 MainMenu.tscn**

`scenes/MainMenu.tscn`（极简 Control 根 + 脚本，与 RouteScene.tscn 同格式）：

```
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://src/ui/main_menu.gd" id="1"]

[node name="MainMenu" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1")
```

- [ ] **Step 5: 改 main_scene**

`project.godot` 的 `[application]` 段，把
```
run/main_scene="res://scenes/RouteScene.tscn"
```
改为
```
run/main_scene="res://scenes/MainMenu.tscn"
```

- [ ] **Step 6: 跑测试确认通过（GREEN）**

```bash
cd "/Users/chris/Documents/First Codex/my-game"
"/Applications/Godot.app/Contents/MacOS/Godot" --headless --import 2>&1 | tail -2
"/Applications/Godot.app/Contents/MacOS/Godot" --headless -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode --continue -a res://tests/integration/main_menu 2>&1 | grep -iE "Statistics|FAILED|SCRIPT ERROR" | tail -6
```
Expected: 5/5 PASSED。

- [ ] **Step 7: 全量回归**

```bash
cd "/Users/chris/Documents/First Codex/my-game"
"/Applications/Godot.app/Contents/MacOS/Godot" --headless -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode --continue -a res://tests 2>&1 | grep -iE "Overall Summary|Executed test|Abnormal|SCRIPT ERROR" | tail -6
```
Expected: `Overall Summary: 305 test cases | 0 errors | 0 failures | 0 flaky | 0 skipped | 0 orphans`（300 + 5）。以实际为准，关键 0 失败/错误/孤儿。

- [ ] **Step 8: 提交**

```bash
cd "/Users/chris/Documents/First Codex/my-game"
git add src/ui/main_menu.gd scenes/MainMenu.tscn project.godot tests/integration/main_menu/
git commit -F - <<'EOF'
feat(ui): whitebox main menu as boot scene

MainMenu (title + 悬赏解锁 N/3 + 出航 + 退出) becomes main_scene. 出航 →
SceneManager.goto_route() (RouteScene IDLE begins a run); 退出 →
get_tree().quit(). Navigation/quit go through injectable Callable seams
so tests don't change scenes or quit the runner. SceneManager /
RouteScene unchanged.

Full suite 305/305 PASSED (was 300).

Story: main-menu

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
git log --oneline -1
```

## Self-Review

**Spec coverage:**
- Rule 1 MainMenu 场景 + main_scene 改 → Step 3/4/5；SceneManager 不改 → 确认无改动。✓
- Rule 2 内容（标题/解锁标签/出航/退出，先 add_child 后 set_anchors）→ Step 3。✓
- Rule 3 出航 → goto_route（RouteScene IDLE）→ `_default_set_sail`。✓
- Rule 4 退出 → quit → `_default_quit`。✓
- Rule 5 DI 接缝（_nav_set_sail/_nav_quit 默认 + _ready 赋值 + _on_* 调用）→ Step 3。✓
- AC-1 渲染+0/3 → test_renders；AC-2 反映 MetaProgress → test_unlock_progress；AC-3 出航接缝 → test_set_sail；AC-4 退出接缝 → test_quit；AC-5 main_scene → test_main_scene；AC-6 全量 → Step 7。✓
- 边界（0/3、N/3、quit 接缝不退运行器）→ 测试 before/after 清 MetaProgress + 接缝覆盖。✓

**Placeholder scan:** 无 TBD/TODO；每步完整代码/命令+期望。✓

**Type consistency:**
- `_set_sail_button/_quit_button/_unlock_label/_nav_set_sail/_nav_quit` + `_on_set_sail/_on_quit/_default_set_sail/_default_quit` 在 Step 3 定义、测试一致引用。✓
- `var ids: Array[String] = [...]` 赋 `unlocked_crew_ids` 规避 Variant；`var called := [0]`(Array,非 Variant) 合法；`ProjectSettings.get_setting` 结果 `str()` 包装规避不安全访问。✓
- 解锁标签格式 `"悬赏解锁 %d / %d"` 与 AC-1/AC-2 断言文本（"悬赏解锁 0 / 3" / "悬赏解锁 2 / 3"）一致；M=3 依赖 bounty 的 3 个 unlockable（已入库）。✓
- MainMenu.tscn ext_resource path 与脚本路径一致；main_scene 字符串与 AC-5 断言一致。✓
