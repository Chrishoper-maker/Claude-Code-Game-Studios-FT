# 寒霜套装（②b-3）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地第 8 套 set_frost 冻结型控制 debuff：命中给敌施寒霜，敌方下回合结算时生效消费——滞步（移动减半）/冰封（不能移动）/冻结（跳过回合），解冻后一回合免疫防永冻。

**Architecture:** 施加复用 ②b-2b 的 `SetReactionSystem`（attack_executed 攻击者侧，照处决先例）。寒霜状态存于既有 `BattleResolution._unit_statuses`；新 `resolve_frost_for_turn` 在敌方单位回合开始时算 outcome+消费+置免疫，无寒霜时清免疫。`EnemyAI._on_enemy_turn_started` 调它：冻结→强制 WAIT，否则把 move_cap 作为有效移动范围决策。FROST_x 回合级（clear_round_statuses 清），FROST_IMMUNE 跨回合（不清）。`SetEffectCatalog` 扩描述。

**Tech Stack:** Godot 4.6.3 / GDScript / GdUnit4。战斗系统节点 + EventBus + 装备套装数据。

## Global Constraints

- 引擎 Godot 4.6.3；测试 GdUnit4：先 `/Applications/Godot.app/Contents/MacOS/Godot --headless --import`，再 `/Applications/Godot.app/Contents/MacOS/Godot --headless -s res://addons/gdUnit4/bin/GdUnitCmdTool.gd --ignoreHeadlessMode --continue -a <test path>`。
- 命名：类 PascalCase、变量/函数 snake_case、常量 UPPER_SNAKE_CASE。
- GDScript 静态类型；公共/新增 API 中文 doc 注释（沿用全库惯例：`#` 行注释）。
- 数值/状态名用具名常量（不散落魔法数/魔法串）。
- 测试 0 孤儿：Node 派生对象（GridBoard/TurnManager/BattleResolution/SetReactionSystem/EnemyAI）须 `auto_free()` 或 after_test `free()`（含未入树的 GridBoard）。
- 提交 Conventional Commits，body 引用 `Story: frost-set`，结尾 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- set_id = `set_frost`（②b-1 数据已含 9 槽 72 件之一）。阈值 [3,6,9]，升级轴取最高激活档（施加时挂单一最高档状态）。
- 施加**阵营无关**（不过滤 faction）；寒霜效果仅在敌方 AI 回合结算。
- 寒霜**不造成伤害**（纯控制，不走 apply_reaction_damage）。
- **不改** SetEffectSystem、`_compute_attack_damage`、伤害管线、PlayerTurnController。既有 EnemyAI 意图逻辑（目标选择/行为分支/攻击判定）行为不变。

## File Structure

- `src/battle/battle_resolution.gd`（改）：加 FROST_* 状态常量 + `resolve_frost_for_turn` + `clear_round_statuses` 扩 3 个 FROST_x。
- `src/battle/set_reaction_system.gd`（改）：`on_attack_executed` 攻击者侧加寒霜分支 + `_apply_frost` + `FROST_STATUS_BY_TIER` 常量。
- `src/ai/enemy_ai.gd`（改）：`_on_enemy_turn_started` 加寒霜短路；新 `_move_cap` 字段 + `_eff_move` 访问器，替换 3 处 `u.get_move_range()`。
- `src/ui/set_effect_catalog.gd`（改）：`_DESC` 加 `set_frost`。
- 测试：`tests/unit/battle_resolution/frost_resolution_test.gd`、`tests/unit/set_reactions/set_reaction_frost_test.gd`、`tests/unit/enemy_ai/enemy_ai_frost_test.gd`、`tests/unit/set_effects/set_effect_catalog_frost_test.gd`、`tests/integration/frost/frost_e2e_test.gd`。

---

### Task 1: BattleResolution 寒霜状态 + resolve_frost_for_turn

**Files:**
- Modify: `src/battle/battle_resolution.gd`
- Test: `tests/unit/battle_resolution/frost_resolution_test.gd` (create)

**Interfaces:**
- Consumes: 既有 `get_unit_status` / `apply_status` / `_consume_status` / `_turn_manager.get_unit` / `UnitInstance.get_move_range`。
- Produces:
  - 常量 `STATUS_FROST_SLOW` / `STATUS_FROST_ROOT` / `STATUS_FROST_FREEZE` / `STATUS_FROST_IMMUNE`（StringName）。
  - `resolve_frost_for_turn(unit_id: int) -> Dictionary`，返回 `{"skip": bool, "move_cap": int}`（move_cap=-1 表正常）。
  - `clear_round_statuses` 扩展清除 FROST_SLOW/ROOT/FREEZE（不清 FROST_IMMUNE）。

- [ ] **Step 1: 写失败测试**

创建 `tests/unit/battle_resolution/frost_resolution_test.gd`：

```gdscript
# resolve_frost_for_turn：冻结/冰封/滞步 outcome + 消费 + 置免疫；无寒霜清免疫；
# FROST_x 回合级清除而 FROST_IMMUNE 跨回合保留。
extends GdUnitTestSuite

var _gb: GridBoard
var _tm: TurnManager
var _br: BattleResolution

func before_test() -> void:
	_gb = GridBoard.new(); _tm = TurnManager.new(); _br = BattleResolution.new()
	add_child(_tm); add_child(_br)
	_br.setup(_gb, _tm)

func after_test() -> void:
	_tm.free(); _br.free(); _gb.free()

func _register(move_range: int) -> int:
	var d := UnitDefinition.new()
	d.id = "e"; d.faction = "enemy"; d.unit_class = "swordsman"
	d.move_range = move_range; d.attack_range = 1; d.base_damage = 3; d.max_hp = 6
	var u := UnitInstance.from_definition(d)
	return _tm.register_unit(u)

func test_freeze_returns_skip_and_consumes_and_immunes() -> void:
	var id := _register(4)
	_br.apply_status(id, BattleResolution.STATUS_FROST_FREEZE)
	var fr := _br.resolve_frost_for_turn(id)
	assert_bool(fr["skip"]).is_true()
	assert_bool(_br.get_unit_status(id, BattleResolution.STATUS_FROST_FREEZE)).is_false()   # 已消费
	assert_bool(_br.get_unit_status(id, BattleResolution.STATUS_FROST_IMMUNE)).is_true()    # 置免疫

func test_root_returns_move_cap_zero() -> void:
	var id := _register(4)
	_br.apply_status(id, BattleResolution.STATUS_FROST_ROOT)
	var fr := _br.resolve_frost_for_turn(id)
	assert_bool(fr["skip"]).is_false()
	assert_int(fr["move_cap"]).is_equal(0)
	assert_bool(_br.get_unit_status(id, BattleResolution.STATUS_FROST_ROOT)).is_false()

func test_slow_returns_half_move_cap() -> void:
	var id := _register(4)
	_br.apply_status(id, BattleResolution.STATUS_FROST_SLOW)
	var fr := _br.resolve_frost_for_turn(id)
	assert_bool(fr["skip"]).is_false()
	assert_int(fr["move_cap"]).is_equal(2)   # floor(4/2)
	assert_bool(_br.get_unit_status(id, BattleResolution.STATUS_FROST_SLOW)).is_false()

func test_no_frost_clears_immune_and_returns_normal() -> void:
	var id := _register(4)
	_br.apply_status(id, BattleResolution.STATUS_FROST_IMMUNE)
	var fr := _br.resolve_frost_for_turn(id)
	assert_bool(fr["skip"]).is_false()
	assert_int(fr["move_cap"]).is_equal(-1)
	assert_bool(_br.get_unit_status(id, BattleResolution.STATUS_FROST_IMMUNE)).is_false()   # 免疫到期

func test_clear_round_statuses_clears_frost_x_not_immune() -> void:
	var id := _register(4)
	_br.apply_status(id, BattleResolution.STATUS_FROST_SLOW)
	_br.apply_status(id, BattleResolution.STATUS_FROST_IMMUNE)
	_br.clear_round_statuses()
	assert_bool(_br.get_unit_status(id, BattleResolution.STATUS_FROST_SLOW)).is_false()    # 回合级清除
	assert_bool(_br.get_unit_status(id, BattleResolution.STATUS_FROST_IMMUNE)).is_true()   # 跨回合保留
```

- [ ] **Step 2: 跑测试确认失败**

Run: `/Applications/Godot.app/Contents/MacOS/Godot --headless --import` 然后 `... -a res://tests/unit/battle_resolution/frost_resolution_test.gd`
Expected: FAIL（STATUS_FROST_* / resolve_frost_for_turn 未定义）。

- [ ] **Step 3: 实现**

在 `battle_resolution.gd` 状态常量区（`STATUS_SET_GUARD` 之后）加：

```gdscript
const STATUS_FROST_SLOW := &"FROST_SLOW"       # 寒霜3档：移动减半（敌回合消费）
const STATUS_FROST_ROOT := &"FROST_ROOT"       # 寒霜6档：不能移动（敌回合消费）
const STATUS_FROST_FREEZE := &"FROST_FREEZE"   # 寒霜9档：跳过整回合（敌回合消费）
const STATUS_FROST_IMMUNE := &"FROST_IMMUNE"   # 解冻后一回合免疫（跨回合，防永冻）
```

在 `clear_round_statuses` 循环体内（现有 `erase(STATUS_SET_GUARD)` 之后）加三行（**不**加 FROST_IMMUNE）：

```gdscript
		_unit_statuses[id].erase(STATUS_FROST_SLOW)
		_unit_statuses[id].erase(STATUS_FROST_ROOT)
		_unit_statuses[id].erase(STATUS_FROST_FREEZE)
```

在 `clear_round_statuses` 之后加新方法：

```gdscript
# 敌方单位回合开始结算寒霜：返回 {skip, move_cap}（move_cap=-1 表正常用 get_move_range）。
# 有寒霜→算 outcome（冻结跳过/冰封move0/滞步move减半）+消费该状态+置 FROST_IMMUNE；
# 无寒霜→清 FROST_IMMUNE（免疫到期）。FROST_x 回合级、FROST_IMMUNE 跨回合（见 clear_round_statuses）。
func resolve_frost_for_turn(unit_id: int) -> Dictionary:
	var u := _turn_manager.get_unit(unit_id)
	if u == null:
		return {"skip": false, "move_cap": -1}
	if get_unit_status(unit_id, STATUS_FROST_FREEZE):
		_consume_status(unit_id, STATUS_FROST_FREEZE)
		apply_status(unit_id, STATUS_FROST_IMMUNE)
		return {"skip": true, "move_cap": 0}
	if get_unit_status(unit_id, STATUS_FROST_ROOT):
		_consume_status(unit_id, STATUS_FROST_ROOT)
		apply_status(unit_id, STATUS_FROST_IMMUNE)
		return {"skip": false, "move_cap": 0}
	if get_unit_status(unit_id, STATUS_FROST_SLOW):
		_consume_status(unit_id, STATUS_FROST_SLOW)
		apply_status(unit_id, STATUS_FROST_IMMUNE)
		return {"skip": false, "move_cap": u.get_move_range() / 2}   # int 除法 = floor
	_consume_status(unit_id, STATUS_FROST_IMMUNE)   # 无寒霜 → 免疫到期
	return {"skip": false, "move_cap": -1}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `... -a res://tests/unit/battle_resolution/frost_resolution_test.gd`
Expected: PASS (5/5)。

- [ ] **Step 5: 提交**

```bash
git add src/battle/battle_resolution.gd tests/unit/battle_resolution/frost_resolution_test.gd
git commit -m "feat(battle): frost statuses + resolve_frost_for_turn (freeze/root/slow + immune)

Story: frost-set
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: SetReactionSystem 寒霜施加

**Files:**
- Modify: `src/battle/set_reaction_system.gd`
- Test: `tests/unit/set_reactions/set_reaction_frost_test.gd` (create)

**Interfaces:**
- Consumes: 框架（on_attack_executed）+ `BattleResolution.apply_status` / `get_unit_status` / `STATUS_FROST_*`（Task 1）+ `SetBonus`。
- Produces: 常量 `FROST_STATUS_BY_TIER`（Dictionary {9:FREEZE,6:ROOT,3:SLOW}）；私有 `_apply_frost`；`on_attack_executed` 攻击者侧加寒霜分支。

- [ ] **Step 1: 写失败测试**

创建 `tests/unit/set_reactions/set_reaction_frost_test.gd`：

```gdscript
# 寒霜套（看攻击者）：命中给非免疫敌方按最高档施寒霜状态；免疫则跳过。
extends GdUnitTestSuite

var _gb: GridBoard
var _tm: TurnManager
var _br: BattleResolution
var _srs: SetReactionSystem
const _SLOTKEYS := ["mainweapon", "offweapon", "head", "armor", "gloves", "legs", "boots", "ring", "necklace"]

func before_test() -> void:
	_gb = GridBoard.new(); _tm = TurnManager.new(); _br = BattleResolution.new(); _srs = SetReactionSystem.new()
	add_child(_tm); add_child(_br); add_child(_srs)
	_br.setup(_gb, _tm); _srs.setup(_gb, _tm, _br)

func after_test() -> void:
	_tm.free(); _br.free(); _srs.free(); _gb.free()

func _crew_def() -> UnitDefinition:
	for d in UnitDataManager.get_all_units():
		if d is CrewDefinition: return d
	return null

func _register_frost(k: int) -> int:
	var eq: Dictionary = {}
	for i in range(k):
		var def := EquipmentDataManager.get_equipment("eq_frost_%s" % _SLOTKEYS[i])
		eq[def.slot] = def
	return _tm.register_unit(UnitInstance.from_definition(_crew_def(), eq))

func _register_plain() -> int:
	return _tm.register_unit(UnitInstance.from_definition(_crew_def(), {}))

func test_frost_3_applies_slow() -> void:
	var aid := _register_frost(3)
	var tid := _register_plain()
	_srs.on_attack_executed(aid, tid, 3)
	assert_bool(_br.get_unit_status(tid, BattleResolution.STATUS_FROST_SLOW)).is_true()

func test_frost_6_applies_root() -> void:
	var aid := _register_frost(6)
	var tid := _register_plain()
	_srs.on_attack_executed(aid, tid, 3)
	assert_bool(_br.get_unit_status(tid, BattleResolution.STATUS_FROST_ROOT)).is_true()

func test_frost_9_applies_freeze() -> void:
	var aid := _register_frost(9)
	var tid := _register_plain()
	_srs.on_attack_executed(aid, tid, 3)
	assert_bool(_br.get_unit_status(tid, BattleResolution.STATUS_FROST_FREEZE)).is_true()

func test_frost_skips_immune_target() -> void:
	var aid := _register_frost(9)
	var tid := _register_plain()
	_br.apply_status(tid, BattleResolution.STATUS_FROST_IMMUNE)
	_srs.on_attack_executed(aid, tid, 3)
	assert_bool(_br.get_unit_status(tid, BattleResolution.STATUS_FROST_FREEZE)).is_false()   # 免疫挡掉
```

- [ ] **Step 2: 跑测试确认失败**

Run: `... -a res://tests/unit/set_reactions/set_reaction_frost_test.gd`
Expected: FAIL（无寒霜分支 → 目标无寒霜状态）。

- [ ] **Step 3: 实现**

`set_reaction_system.gd` 常量区（`EXECUTIONER` 之后）加：

```gdscript
const FROST_STATUS_BY_TIER := {
	9: BattleResolution.STATUS_FROST_FREEZE,
	6: BattleResolution.STATUS_FROST_ROOT,
	3: BattleResolution.STATUS_FROST_SLOW,
}
```

`on_attack_executed` 攻击者侧（`_apply_executioner` 调用之后、`if attacker != null` 块内）加：

```gdscript
			if SetBonus.count_sets(attacker).has("set_frost"):
				_apply_frost(attacker, target_id)
```

加方法（放在 `_apply_executioner` 之后）：

```gdscript
# 寒霜：命中给非免疫敌方按最高激活档施寒霜状态（滞步/冰封/冻结）；不造成伤害。
func _apply_frost(attacker: UnitInstance, target_id: int) -> void:
	var t := _turn_manager.get_unit(target_id)
	if t == null or not t.is_alive or t.current_hp <= 0:
		return
	if _battle_resolution.get_unit_status(target_id, BattleResolution.STATUS_FROST_IMMUNE):
		return
	for tier in [9, 6, 3]:
		if SetBonus.is_tier_active(attacker, "set_frost", tier):
			_battle_resolution.apply_status(target_id, FROST_STATUS_BY_TIER[tier])
			return
```

- [ ] **Step 4: 跑测试确认通过**

Run: `... -a res://tests/unit/set_reactions/set_reaction_frost_test.gd`
Expected: PASS (4/4)。

- [ ] **Step 5: 提交**

```bash
git add src/battle/set_reaction_system.gd tests/unit/set_reactions/set_reaction_frost_test.gd
git commit -m "feat(battle): frost set application (on-hit, immune-gated, highest tier)

Story: frost-set
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: EnemyAI 寒霜整合（短路 + 有效移动范围）

**Files:**
- Modify: `src/ai/enemy_ai.gd`
- Test: `tests/unit/enemy_ai/enemy_ai_frost_test.gd` (create)

**Interfaces:**
- Consumes: `BattleResolution.resolve_frost_for_turn` / `apply_status` / `STATUS_FROST_*`（Task 1）；既有 `_turn_manager.mark_has_acted` / `EventBus.enemy_actions_completed`。
- Produces: 字段 `_move_cap`（int，默认 -1）；私有 `_eff_move(u) -> int`；`_on_enemy_turn_started` 寒霜短路；`_engage`/`_greedy_approach`/`_retreat_from` 用 `_eff_move(u)` 取代 `u.get_move_range()`。

- [ ] **Step 1: 写失败测试**

创建 `tests/unit/enemy_ai/enemy_ai_frost_test.gd`：

```gdscript
# EnemyAI 寒霜整合：冻结→不行动(WAIT);冰封→可攻不可移;滞步→移动范围减半。
# 经 _on_enemy_turn_started 驱真实意图执行（resolve_frost_for_turn 消费+免疫）。
extends GdUnitTestSuite

var _gb: GridBoard
var _tm: TurnManager
var _br: BattleResolution
var _ai: EnemyAI
var _uid: int

func before_test() -> void:
	if EventBus.battle_won.is_connected(RunManager._on_battle_won):
		EventBus.battle_won.disconnect(RunManager._on_battle_won)
	_gb = auto_free(GridBoard.new())
	_tm = auto_free(TurnManager.new())
	_br = auto_free(BattleResolution.new())
	_br.setup(_gb, _tm)
	_ai = auto_free(EnemyAI.new())
	_ai.setup(_gb, _tm, _br)
	_uid = 0

func after_test() -> void:
	if not EventBus.battle_won.is_connected(RunManager._on_battle_won):
		EventBus.battle_won.connect(RunManager._on_battle_won)

func _add(faction: String, pos: Vector2i, move_range: int) -> int:
	_uid += 1
	var d := UnitDefinition.new()
	d.id = "u%d" % _uid; d.faction = faction; d.unit_class = "swordsman"
	d.move_range = move_range; d.attack_range = 1; d.base_damage = 3; d.max_hp = 6
	var inst := UnitInstance.from_definition(d)
	inst.current_hp = 6; inst.grid_position = pos; inst.behavior_type = "MELEE"
	var bid := _tm.register_unit(inst)
	_gb.place_unit(bid, pos)
	return bid

func test_freeze_makes_enemy_skip_turn() -> void:
	var enemy := _add("enemy", Vector2i(3, 3), 4)
	var crew := _add("crew", Vector2i(3, 2), 4)   # 相邻，正常会被攻击
	_br.apply_status(enemy, BattleResolution.STATUS_FROST_FREEZE)
	_ai._on_enemy_turn_started(enemy)
	assert_int(_tm.get_unit(crew).current_hp).is_equal(6)                 # 未被攻击
	assert_vector(_tm.get_unit(enemy).grid_position).is_equal(Vector2i(3, 3))  # 未移动
	assert_bool(_br.get_unit_status(enemy, BattleResolution.STATUS_FROST_IMMUNE)).is_true()

func test_root_allows_attack_but_no_move() -> void:
	var enemy := _add("enemy", Vector2i(3, 3), 4)
	var crew := _add("crew", Vector2i(3, 2), 4)   # 相邻
	_br.apply_status(enemy, BattleResolution.STATUS_FROST_ROOT)
	_ai._on_enemy_turn_started(enemy)
	assert_int(_tm.get_unit(crew).current_hp).is_less(6)                  # 相邻仍可攻
	assert_vector(_tm.get_unit(enemy).grid_position).is_equal(Vector2i(3, 3))  # 未移动

func test_root_cannot_approach_distant_target() -> void:
	var enemy := _add("enemy", Vector2i(3, 5), 4)
	_add("crew", Vector2i(3, 0), 4)   # 远，正常会接近
	_br.apply_status(enemy, BattleResolution.STATUS_FROST_ROOT)
	_ai._on_enemy_turn_started(enemy)
	assert_vector(_tm.get_unit(enemy).grid_position).is_equal(Vector2i(3, 5))  # 不能移动

func test_slow_halves_move_range() -> void:
	var enemy := _add("enemy", Vector2i(3, 6), 4)
	_add("crew", Vector2i(3, 0), 4)   # 远（曼哈顿6），正常 move4 接近
	_br.apply_status(enemy, BattleResolution.STATUS_FROST_SLOW)
	_ai._on_enemy_turn_started(enemy)
	# 有效移动 floor(4/2)=2：从 (3,6) 最多移 2 格逼近 → y 不低于 4
	var moved := _tm.get_unit(enemy).grid_position
	assert_int(moved.y).is_greater_equal(4)
	assert_int(GridBoard.manhattan(Vector2i(3, 6), moved)).is_less_equal(2)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `... -a res://tests/unit/enemy_ai/enemy_ai_frost_test.gd`
Expected: FAIL（无寒霜短路 → 冻结敌仍攻击/移动）。

- [ ] **Step 3: 实现**

`enemy_ai.gd`：在字段区（`var _intent_map` 之后）加：

```gdscript
var _move_cap := -1   # 本回合寒霜有效移动上限（-1=不限，用 get_move_range）
```

把 `_on_enemy_turn_started` 改为（开头加寒霜短路，结尾复位 _move_cap）：

```gdscript
func _on_enemy_turn_started(unit_id: int) -> void:
	var fr := _battle_resolution.resolve_frost_for_turn(unit_id)
	if fr["skip"]:                                    # 冻结：整回合跳过
		_turn_manager.mark_has_acted(unit_id)
		EventBus.enemy_actions_completed.emit(unit_id)
		return
	_move_cap = int(fr["move_cap"])                   # 冰封=0 / 滞步=减半 / 正常=-1
	var intent := decide_intent(unit_id)
	EventBus.intent_declared.emit(unit_id, intent)
	_execute_intent(intent)
	_move_cap = -1
	EventBus.enemy_actions_completed.emit(unit_id)
```

在 `decide_intent` 之前（`# ── Rule 1/2 意图决策 ──` 注释后）加访问器：

```gdscript
# 本回合有效移动范围：寒霜 move_cap≥0 时取之（冰封0/滞步减半），否则单位基础移动。
func _eff_move(u: UnitInstance) -> int:
	return _move_cap if _move_cap >= 0 else u.get_move_range()
```

把 `_engage` / `_greedy_approach` / `_retreat_from` 中**三处** `u.get_move_range()` 替换为 `_eff_move(u)`：

```gdscript
	# _engage 内：
	var staging := _grid_board.get_attack_staging_cells(u.grid_position, _eff_move(u), t.grid_position, u.get_attack_range())
```
```gdscript
	# _greedy_approach 内：
	for cell in _grid_board.get_reachable_cells(u.grid_position, _eff_move(u)):
```
```gdscript
	# _retreat_from 内：
	for cell in _grid_board.get_reachable_cells(u.grid_position, _eff_move(u)):
```

- [ ] **Step 4: 跑测试确认通过**

Run: `... -a res://tests/unit/enemy_ai/enemy_ai_frost_test.gd`
Expected: PASS (4/4)。

- [ ] **Step 5: 既有 EnemyAI 测试回归**

Run: `... -a res://tests/unit/enemy_ai`
Expected: 全 PASS（_eff_move 默认 -1 → 行为与 get_move_range 一致，既有意图测试不受影响）。

- [ ] **Step 6: 提交**

```bash
git add src/ai/enemy_ai.gd tests/unit/enemy_ai/enemy_ai_frost_test.gd
git commit -m "feat(ai): EnemyAI honors frost (freeze=skip, root/slow=effective move range)

Story: frost-set
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: SetEffectCatalog 寒霜描述

**Files:**
- Modify: `src/ui/set_effect_catalog.gd`
- Test: `tests/unit/set_effects/set_effect_catalog_frost_test.gd` (create)

**Interfaces:**
- Produces: `SetEffectCatalog._DESC` 补 `set_frost` 各 {3,6,9}。

- [ ] **Step 1: 写失败测试**

创建 `tests/unit/set_effects/set_effect_catalog_frost_test.gd`：

```gdscript
# SetEffectCatalog：寒霜套各档有中文描述。
extends GdUnitTestSuite

func test_frost_set_has_descriptions() -> void:
	assert_str(SetEffectCatalog.describe("set_frost", 3)).is_equal("滞步")
	assert_str(SetEffectCatalog.describe("set_frost", 6)).is_equal("冰封")
	assert_str(SetEffectCatalog.describe("set_frost", 9)).is_equal("冻结")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `... -a res://tests/unit/set_effects/set_effect_catalog_frost_test.gd`
Expected: FAIL（set_frost 描述为 ""）。

- [ ] **Step 3: 实现**

`set_effect_catalog.gd` 的 `_DESC` 字典加一键（在现有反应三套条目之后，缩进对齐既有条目）：

```gdscript
	"set_frost": {3: "滞步", 6: "冰封", 9: "冻结"},
```

- [ ] **Step 4: 跑测试确认通过**

Run: `... -a res://tests/unit/set_effects/set_effect_catalog_frost_test.gd`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add src/ui/set_effect_catalog.gd tests/unit/set_effects/set_effect_catalog_frost_test.gd
git commit -m "feat(ui): catalog descriptions for frost set

Story: frost-set
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: 端到端集成 + 全量回归

**Files:**
- Test: `tests/integration/frost/frost_e2e_test.gd` (create)

**Interfaces:**
- Consumes: 全链（execute_attack → attack_executed → SetReactionSystem 施寒霜 → resolve_frost_for_turn 结算）。

- [ ] **Step 1: 写端到端测试**

创建 `tests/integration/frost/frost_e2e_test.gd`：

```gdscript
# 端到端：crew 持寒霜真打敌→施寒霜状态→敌回合 resolve 结算消费+免疫；免疫挡掉次回合施加。
extends GdUnitTestSuite

var _gb: GridBoard
var _tm: TurnManager
var _br: BattleResolution
var _srs: SetReactionSystem
const _SLOTKEYS := ["mainweapon", "offweapon", "head", "armor", "gloves", "legs", "boots", "ring", "necklace"]

func before_test() -> void:
	_gb = GridBoard.new(); _tm = TurnManager.new(); _br = BattleResolution.new(); _srs = SetReactionSystem.new()
	add_child(_tm); add_child(_br); add_child(_srs)
	_br.setup(_gb, _tm); _srs.setup(_gb, _tm, _br)

func after_test() -> void:
	_tm.free(); _br.free(); _srs.free(); _gb.free()

func _crew_def() -> UnitDefinition:
	for d in UnitDataManager.get_all_units():
		if d is CrewDefinition: return d
	return null

func _register_frost9(pos: Vector2i) -> int:
	var eq: Dictionary = {}
	for i in range(9):
		var ed := EquipmentDataManager.get_equipment("eq_frost_%s" % _SLOTKEYS[i])
		eq[ed.slot] = ed
	var u := UnitInstance.from_definition(_crew_def(), eq)
	var id := _tm.register_unit(u); u.grid_position = pos; _gb.place_unit(id, pos)
	return id

func _register_enemy(pos: Vector2i) -> int:
	var d := UnitDefinition.new()
	d.id = "e"; d.faction = "enemy"; d.unit_class = "swordsman"
	d.move_range = 4; d.attack_range = 1; d.base_damage = 3; d.max_hp = 20
	var u := UnitInstance.from_definition(d)
	u.current_hp = 20
	var id := _tm.register_unit(u); u.grid_position = pos; _gb.place_unit(id, pos)
	return id

func test_attack_applies_freeze_then_resolved_and_immune() -> void:
	var crew := _register_frost9(Vector2i(0, 0))
	var enemy := _register_enemy(Vector2i(1, 0))
	_br.execute_attack(crew, enemy)                              # 真信号链 → 施冻结
	assert_bool(_br.get_unit_status(enemy, BattleResolution.STATUS_FROST_FREEZE)).is_true()
	var fr := _br.resolve_frost_for_turn(enemy)                  # 敌回合结算
	assert_bool(fr["skip"]).is_true()
	assert_bool(_br.get_unit_status(enemy, BattleResolution.STATUS_FROST_FREEZE)).is_false()   # 消费
	assert_bool(_br.get_unit_status(enemy, BattleResolution.STATUS_FROST_IMMUNE)).is_true()    # 免疫

func test_immune_blocks_next_application() -> void:
	var crew := _register_frost9(Vector2i(0, 0))
	var enemy := _register_enemy(Vector2i(1, 0))
	_br.apply_status(enemy, BattleResolution.STATUS_FROST_IMMUNE)
	_br.execute_attack(crew, enemy)
	assert_bool(_br.get_unit_status(enemy, BattleResolution.STATUS_FROST_FREEZE)).is_false()   # 免疫挡掉施加
```

- [ ] **Step 2: 跑该测试确认通过**

Run: `/Applications/Godot.app/Contents/MacOS/Godot --headless --import` 然后 `... -a res://tests/integration/frost/frost_e2e_test.gd`
Expected: PASS (2/2)。

- [ ] **Step 3: 全量回归**

Run: `... -a res://tests`
Expected: 全 PASS，0 errors / 0 failures / 0 orphans。捕获 Overall Summary 行。

- [ ] **Step 4: 提交**

```bash
git add tests/integration/frost/frost_e2e_test.gd
git commit -m "test(frost): e2e (execute_attack→apply→resolve→immune) + full regression

Story: frost-set
Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**Spec coverage：**
- §3.1 施加模型（SetReactionSystem 攻击者侧，免疫门控，普攻/斩触发，阵营无关）→ Task 2。✅
- §3.2 三档阶梯（FROST_SLOW/ROOT/FREEZE，最高档）→ Task 1（常量）+ Task 2（施加）+ Task 3（生效）。✅
- §3.3 结算+防永冻（resolve_frost_for_turn outcome/消费/置免疫；无寒霜清免疫）→ Task 1。✅
- §3.4 状态生命周期（FROST_x 回合级 clear、FROST_IMMUNE 跨回合）→ Task 1（clear_round_statuses + resolve 管理）。✅
- §3.5 整合（EnemyAI 短路 + 有效移动范围）→ Task 3。✅
- §3.6 纸娃娃描述 → Task 4。✅
- §5 边界（免疫跳过/死亡清状态[resolve_unit_downed 既有 erase]/未攻击清免疫/多次命中幂等/damage==0/floor 低移动/冻结优先）→ Task 1/2/3 实现 + 测试覆盖（免疫跳过 T2/T5；floor=move/2 T1；冻结优先 T3）。✅
- §8 AC-1..9 → AC1 T2 / AC2-4 T1+T3 / AC5 T1+T2+T5 / AC6 T1 / AC7 T3 / AC8 T4 / AC9 T5(全量回归)+T3(既有 EnemyAI 回归)。✅
- §9 非目标：未碰 PlayerTurnController/层数/伤害/概率/难度/美术。✅

**Placeholder 扫描：** 无 TBD/TODO。每步含完整代码。

**类型一致性：** `STATUS_FROST_*`（Task1 定义，Task2/3/5 消费）一致；`resolve_frost_for_turn(unit_id)->Dictionary{skip,move_cap}`（Task1 定义，Task3 消费 fr["skip"]/fr["move_cap"]）一致；`FROST_STATUS_BY_TIER`（Task2）键 9/6/3 对齐 STATUS 常量；`_eff_move(u)`/`_move_cap`（Task3 内部）一致；`SetEffectCatalog.describe`（既有，Task4 扩数据）。set_id "set_frost" 统一。

**风险备注（执行者注意）：**
- Task 3 改 `_on_enemy_turn_started` 是行为变更（加寒霜短路），但 `_move_cap` 默认 -1 → `_eff_move` 退化为 `get_move_range()`，无寒霜时行为与现状逐字等价；既有 enemy_ai 两套测试（intent 计算 + 执行循环）须回归绿（Task 3 Step 5）。
- Task 3 测试直接调 `_ai._on_enemy_turn_started(eid)`（白盒），会 emit `enemy_actions_completed`；before_test 已断开 RunManager._on_battle_won 避免 battle_won 耦合（crew hp 足够不触发败北）。
- `resolve_frost_for_turn` 的 move_cap=0（冰封）→ `get_reachable_cells(pos,0)` 返回空（不含起点）→ 不能移动但仍可攻，与 spec 一致（Task 3 test_root_*）。
